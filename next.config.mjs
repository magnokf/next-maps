/** @type {import('next').NextConfig} */
const nextConfig = {
	output: "standalone",
	pageExtensions: ['js', 'jsx', 'ts', 'tsx'], // Add if you are using custom extensions
	
};

export default nextConfig;